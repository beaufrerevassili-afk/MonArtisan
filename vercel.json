{
  "buildCommand": "cd frontend_web && npm install && npm run build",
  "outputDirectory": "frontend_web/dist",
  "framework": "vite",
  "installCommand": "cd frontend_web && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
